## Requisitos

- Java 17

## RUN

cmd:
- `mvn clean package`
- `mvn spring-boot:run`

## Despliegue (Render)

Asegúrate de configurar la variable de entorno `JAVA_VERSION` con el valor `17` en la configuración de tu servicio.